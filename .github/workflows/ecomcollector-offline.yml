name: Build EcomCollector V1.0.4 Offline Installer

on:
  push:
    branches:
      - build/ecomcollector-v1.0.4-offline
    paths:
      - 'ecomcollector-build/source.b64.part*'
      - 'ecomcollector-build/repair/**'
      - 'ecomcollector-build/repair2/**'
      - '.github/workflows/ecomcollector-offline.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-offline-installer:
    runs-on: windows-2022
    timeout-minutes: 90

    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4

      - name: Reassemble and verify EcomCollector source
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $chunks = New-Object System.Collections.Generic.List[string]
          for ($i = 0; $i -le 8; $i++) {
            $id = '{0:D2}' -f $i
            if ($i -eq 1) {
              $value = Get-Content 'ecomcollector-build\repair\part01.0' -Raw
            } elseif ($i -eq 5) {
              $value = ((0..3 | ForEach-Object { Get-Content ("ecomcollector-build\repair2\part05.$_") -Raw }) -join '')
            } elseif ($i -eq 7) {
              $value = ((0..3 | ForEach-Object { Get-Content ("ecomcollector-build\repair2\part07.$_") -Raw }) -join '')
            } else {
              $value = Get-Content ("ecomcollector-build\source.b64.part$id") -Raw
            }
            $chunks.Add(($value -replace '\s', ''))
          }
          $b64 = ($chunks -join '')
          if ($b64.Length -ne 105040) { throw "Unexpected Base64 length: $($b64.Length), expected 105040" }
          [IO.File]::WriteAllBytes('ecomcollector-build\source.zip', [Convert]::FromBase64String($b64))
          $sourceHash = (Get-FileHash 'ecomcollector-build\source.zip' -Algorithm SHA256).Hash.ToLowerInvariant()
          $expectedHash = '56337971722e5fe25a670497260264fc07f0c4aceacd47325318d7c5bfb29eaa'
          if ($sourceHash -ne $expectedHash) { throw "Source ZIP SHA256 mismatch: $sourceHash" }
          Write-Host "Source ZIP SHA256 verified: $sourceHash"
          New-Item -ItemType Directory -Force -Path ecomcollector | Out-Null
          Expand-Archive -Path ecomcollector-build\source.zip -DestinationPath ecomcollector -Force
          if (-not (Test-Path ecomcollector\build_windows.ps1)) { throw 'EcomCollector source extraction failed' }
          Get-Content ecomcollector\src\ecom_collector\__init__.py

          $devReq = 'ecomcollector\requirements-dev.txt'
          $devText = Get-Content $devReq -Raw
          if ($devText -notmatch '(?m)^pytest-asyncio') { Add-Content $devReq "`npytest-asyncio>=0.24,<2" -Encoding UTF8 }

          $legacyOnlineTest = 'ecomcollector\tests\test_windows_online_installer_contract.py'
          if (Test-Path $legacyOnlineTest) { Move-Item $legacyOnlineTest ($legacyOnlineTest + '.legacy') -Force }
          if (-not (Test-Path 'ecomcollector\tests\test_windows_offline_installer_contract.py')) { throw 'V1.0.4 offline installer contract tests missing' }

          $accountTest = 'ecomcollector\tests\test_account_service.py'
          $accountText = Get-Content $accountTest -Raw
          $oldAssert = '    assert row["profile_dir"].endswith("browser_profiles/taobao")'
          $newAssert = '    assert Path(row["profile_dir"]).parts[-2:] == ("browser_profiles", "taobao")'
          if (-not $accountText.Contains($oldAssert)) { throw 'Expected cross-platform profile path assertion not found' }
          Set-Content $accountTest ($accountText.Replace($oldAssert, $newAssert)) -Encoding UTF8

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          architecture: 'x64'
          cache: 'pip'
          cache-dependency-path: 'ecomcollector/requirements-dev.txt'

      - name: Install Inno Setup
        shell: powershell
        timeout-minutes: 10
        run: |
          $ErrorActionPreference = 'Stop'
          choco install innosetup -y --no-progress
          if (-not (Test-Path 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe')) { throw 'Inno Setup 6 missing' }

      - name: Prepare FFmpeg offline payload
        shell: powershell
        timeout-minutes: 10
        run: |
          $ErrorActionPreference = 'Stop'
          choco install ffmpeg -y --no-progress --limit-output
          $toolRoot = 'C:\ProgramData\chocolatey\lib\ffmpeg\tools'
          $ffmpeg = Get-ChildItem -Path $toolRoot -Recurse -Filter ffmpeg.exe -File | Where-Object { $_.Length -gt 1MB } | Sort-Object Length -Descending | Select-Object -First 1
          $ffprobe = Get-ChildItem -Path $toolRoot -Recurse -Filter ffprobe.exe -File | Where-Object { $_.Length -gt 1MB } | Sort-Object Length -Descending | Select-Object -First 1
          if (-not $ffmpeg -or -not $ffprobe) { throw 'Real FFmpeg/ffprobe binaries not found under Chocolatey tools' }
          New-Item -ItemType Directory -Force -Path ecomcollector\vendor\ffmpeg | Out-Null
          Copy-Item $ffmpeg.FullName ecomcollector\vendor\ffmpeg\ffmpeg.exe -Force
          Copy-Item $ffprobe.FullName ecomcollector\vendor\ffmpeg\ffprobe.exe -Force
          $copiedFfmpeg = Get-Item ecomcollector\vendor\ffmpeg\ffmpeg.exe
          $copiedFfprobe = Get-Item ecomcollector\vendor\ffmpeg\ffprobe.exe
          if ($copiedFfmpeg.Length -le 1MB -or $copiedFfprobe.Length -le 1MB) { throw 'FFmpeg payload unexpectedly points to shim binaries' }
          $ffmpegProbe = Start-Process -FilePath $copiedFfmpeg.FullName -ArgumentList '-version' -NoNewWindow -Wait -PassThru
          if ($ffmpegProbe.ExitCode -ne 0) { throw "Copied ffmpeg.exe failed version probe: exit $($ffmpegProbe.ExitCode)" }
          $ffprobeProbe = Start-Process -FilePath $copiedFfprobe.FullName -ArgumentList '-version' -NoNewWindow -Wait -PassThru
          if ($ffprobeProbe.ExitCode -ne 0) { throw "Copied ffprobe.exe failed version probe: exit $($ffprobeProbe.ExitCode)" }
          Write-Host "Real FFmpeg payload: $($copiedFfmpeg.Length) bytes"
          Write-Host "Real FFprobe payload: $($copiedFfprobe.Length) bytes"

      - name: Build Windows x64 application and offline installer
        shell: powershell
        timeout-minutes: 45
        run: |
          $ErrorActionPreference = 'Stop'
          Set-Location ecomcollector
          $env:ECOM_BUILD_PYTHON = (Get-Command python).Source
          .\build_windows.ps1

      - name: Silent-install offline package and run installed self-check
        shell: powershell
        timeout-minutes: 15
        run: |
          $ErrorActionPreference = 'Stop'
          $installer = Get-ChildItem ecomcollector\release\*.exe | Where-Object { $_.Name -match 'V1\.0\.4' } | Select-Object -First 1
          if (-not $installer) { throw 'V1.0.4 installer not found for install probe' }
          $installDir = Join-Path $env:RUNNER_TEMP 'EcomCollectorInstalled'
          if (Test-Path $installDir) { Remove-Item $installDir -Recurse -Force }
          $arguments = @('/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART',('/DIR="' + $installDir + '"'))
          $proc = Start-Process -FilePath $installer.FullName -ArgumentList $arguments -Wait -PassThru
          if ($proc.ExitCode -ne 0) { throw "Offline installer returned exit code $($proc.ExitCode)" }
          $exe = Join-Path $installDir 'EcomCollector.exe'
          if (-not (Test-Path $exe)) { throw 'Installed EcomCollector.exe missing' }
          $ffmpeg = Join-Path $installDir '_internal\vendor\ffmpeg\ffmpeg.exe'
          $ffprobe = Join-Path $installDir '_internal\vendor\ffmpeg\ffprobe.exe'
          if (-not (Test-Path $ffmpeg)) { throw 'Installed ffmpeg.exe missing' }
          if (-not (Test-Path $ffprobe)) { throw 'Installed ffprobe.exe missing' }
          & $exe --self-check
          if ($LASTEXITCODE -ne 0) { throw "Installed application self-check failed: exit $LASTEXITCODE" }
          Write-Host "Offline install probe passed at $installDir"

      - name: Verify offline installer artifact
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $installer = Get-ChildItem ecomcollector\release\*.exe | Where-Object { $_.Name -match 'V1\.0\.4' } | Select-Object -First 1
          if (-not $installer) { throw 'V1.0.4 installer not found' }
          if ($installer.Length -lt 100MB) { throw "Installer unexpectedly small: $($installer.Length) bytes; offline payload may be incomplete" }
          $hash = (Get-FileHash $installer.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
          $report = [ordered]@{
            product = '电商商品采集与素材管理器'
            version = '1.0.4'
            installer_kind = 'full-offline-windows-x64'
            install_time_network_required = $false
            pyinstaller_bundled = $true
            playwright_chromium_bundled = $true
            ffmpeg_bundled = $true
            installer_name = $installer.Name
            size_bytes = $installer.Length
            sha256 = $hash
            build_os = 'windows-2022'
            source_zip_sha256 = '56337971722e5fe25a670497260264fc07f0c4aceacd47325318d7c5bfb29eaa'
            installer_silent_install_verified = $true
            installed_app_self_check_verified = $true
          }
          $report | ConvertTo-Json -Depth 5 | Set-Content ecomcollector\release\V1.0.4-build-report.json -Encoding UTF8
          "$hash  $($installer.Name)" | Set-Content ecomcollector\release\V1.0.4-SHA256SUMS.txt -Encoding ASCII
          Write-Host "Installer: $($installer.FullName)"
          Write-Host "Size: $($installer.Length)"
          Write-Host "SHA256: $hash"

      - name: Upload V1.0.4 offline installer
        uses: actions/upload-artifact@v4
        with:
          name: EcomCollector-V1.0.4-Full-Offline-Windows-x64
          path: |
            ecomcollector/release/*.exe
            ecomcollector/release/V1.0.4-build-report.json
            ecomcollector/release/V1.0.4-SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 7
