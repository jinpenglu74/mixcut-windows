<UserControl x:Class="MixCut.Views.SchemesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:schemes="clr-namespace:MixCut.Views.Schemes"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左：策略列表 -->
        <DockPanel Grid.Column="0">
            <Border DockPanel.Dock="Top" Padding="16,12" BorderBrush="#EEE"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="混剪方案" FontSize="13" FontWeight="SemiBold" />
                        <Border x:Name="CountBadge" Background="#F0F0F2" CornerRadius="999"
                                Padding="7,2" Margin="8,0,0,0" Visibility="Collapsed"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="CountBadgeText" FontSize="10" Foreground="#666" />
                        </Border>
                    </StackPanel>
                    <Button Grid.Column="1" x:Name="GenerateButton" Content="✨ 生成"
                            Padding="12,5" Style="{StaticResource PrimaryButtonStyle}"
                            Click="OnOpenGenerateDialog" />
                </Grid>
            </Border>

            <Border DockPanel.Dock="Top" x:Name="ErrorBanner" Background="#FFF5E0"
                    Padding="14,8" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ErrorText" FontSize="11" Foreground="#7A5800"
                               TextWrapping="Wrap" />
                    <Button Grid.Column="1" Content="✕" Background="Transparent" BorderThickness="0"
                            Padding="6,2" Margin="6,0,0,0" Cursor="Hand"
                            Click="OnDismissError" />
                </Grid>
            </Border>

            <Border DockPanel.Dock="Top" x:Name="ProgressBanner" Background="#E7F0FF"
                    Padding="14,8" Visibility="Collapsed">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="ProgressText" FontSize="11" Foreground="#1D6BE5"
                                   VerticalAlignment="Center" TextWrapping="Wrap" />
                        <Button Grid.Column="1" x:Name="CancelGenerateButton" Content="取消"
                                Click="OnCancelGenerate" Margin="8,0,0,0" Padding="10,2"
                                FontSize="10" Cursor="Hand" Background="White" Foreground="#1D6BE5"
                                BorderBrush="#1D6BE5" BorderThickness="1" VerticalAlignment="Center" />
                    </Grid>
                    <ProgressBar x:Name="GenerateProgressBar" IsIndeterminate="True" Minimum="0" Maximum="1"
                                 Height="3" Margin="0,6,0,0" />
                </StackPanel>
            </Border>

            <ScrollViewer x:Name="StrategyScroll" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="StrategyList" Margin="0,4,0,4" />
            </ScrollViewer>
        </DockPanel>

        <Border Grid.Column="1" Background="#EEE" />

        <!-- 右：方案详情 + drawer overlay -->
        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 原 EmptyDetail / DetailScroller 全部放到 Column=0 -->
            <Grid Grid.Column="0">
                <StackPanel x:Name="EmptyDetail" VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="Visible">
                    <TextBlock Text="📋" FontSize="40" Foreground="#CCC"
                               HorizontalAlignment="Center" Margin="0,0,0,8" />
                    <TextBlock Text="选择一个方案查看详情" Foreground="#888" FontSize="12"
                               HorizontalAlignment="Center" />
                </StackPanel>

                <ScrollViewer x:Name="DetailScroller" VerticalScrollBarVisibility="Auto"
                              Visibility="Collapsed">
                    <StackPanel x:Name="DetailPanel" Margin="24" />
                </ScrollViewer>
            </Grid>

            <!-- Phase 4b：分镜选择抽屉（右侧 320px，默认 Collapsed） -->
            <schemes:SegmentPickerDrawer Grid.Column="1" x:Name="PickerDrawer"
                                         Visibility="Collapsed"/>
        </Grid>
    </Grid>
</UserControl>
